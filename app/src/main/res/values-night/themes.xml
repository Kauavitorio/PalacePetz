<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PalacePetz" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/tab_indicator_gray</item>
        <item name="colorPrimaryVariant">@color/tab_indicator_gray</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/device_presentation_description_color</item>
        <item name="colorSecondaryVariant">@color/device_presentation_description_color</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/background_bottom</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/background_top</item>

        <item name="colorControlActivated">#06AAFF</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Start BottomSheet Theme -->
    <style name="BottomSheetTheme" parent="Theme.Design.BottomSheetDialog">

        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>

    </style>
    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">

        <item name="android:background">@android:color/transparent</item>

    </style>
    <!-- End BottomSheet Theme -->

    <style name="DevicePresentation" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/background_bottom_devicePresentation</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/background_top_devicePresentation</item>
    </style>


    <style name="ProductDetails" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/background_Details_Products</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/bg_product_details</item>
    </style>

    <style name="ShoppingCart" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/background_bottom</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/background_menu_sheet</item>
    </style>

</resources>